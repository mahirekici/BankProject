pipeline {
    agent any

    triggers {
        githubPush(filter: "tags/*")
    }

    stages {
        stage('Build') {
            steps {
                // Build steps
            }
        }
    }
}

